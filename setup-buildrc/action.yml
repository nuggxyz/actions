name: Setup buildrc
description: "setup buildrc in your github action"
runs:
    using: "composite"
    steps:
      - name: artifact
        id: artifact
        uses: actions/download-artifact@v3
        with:
          name: buildrc-override
      - name: script
        shell: bash
        run: |
          # Step 1: Check for buildrc-override
          echo "Checking for buildrc-override... at $override_artifact"
          if [[ -f "$override_artifact" ]]; then
            echo "Override artifact found. Moving..."
            mkdir -p temp
            mv "$override_artifact" ./temp/buildrc
          else
            echo "Override artifact not found. Downloading from GitHub releases..."
            # The name of the release file will be 'os-arch.tar.gz'
            os_arch_pattern="${RUNNER_OS,,}-$RUNNER_ARCH.tar.gz"
            gh release download --pattern "$os_arch_pattern" --repo nuggxyz/buildrc --dir ./temp/
            tar -xzf "./temp/$os_arch_pattern" -C ./temp/
          fi

          # Step 2: Setup alias
          echo "Setting up alias..."
          echo "loading .buildrc"
          echo "$GITHUB_WORKSPACE/temp" >> $GITHUB_PATH
          chmod +x ./temp/buildrc
          ./temp/buildrc load .buildrc
          echo "buildrc loaded ✅"
        env:
          version: v0.1.2
          override_artifact: ${{ steps.artifact.outputs.download-path }}/linux-amd64
